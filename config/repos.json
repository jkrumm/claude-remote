{
  "_comment": "List of repos to clone into ~/SourceRoot/. Uses HTTPS authenticated via gh CLI (see setup/06-setup-gh-cli.sh).",
  "repos": [
    {
      "name": "basalt-ui",
      "url": "https://github.com/jkrumm/basalt-ui.git",
      "description": "Primary test repo for validating the setup"
    }
  ]
}
